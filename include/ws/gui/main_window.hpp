#pragma once

namespace ws::gui {

// Main application window for the graphical user interface.
// Handles initialization and the main rendering loop.
class MainWindow {
public:
    // Runs the main window and returns exit code.
    int run();
};

} // namespace ws::gui
